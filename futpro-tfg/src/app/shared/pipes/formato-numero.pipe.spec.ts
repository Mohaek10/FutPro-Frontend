import { FormatoNumeroPipe } from './formato-numero.pipe';

describe('FormatoNumeroPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoNumeroPipe();
    expect(pipe).toBeTruthy();
  });
});
